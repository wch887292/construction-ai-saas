# 真实大模型配置示例（复制为 .env 并按需修改；.env 已被 gitignore，不会入库）
AI_PROVIDER=llm
LLM_BASE_URL=https://api.deepseek.com/v1
LLM_API_KEY=sk-你的key
LLM_MODEL=deepseek-chat
# 部分兼容服务不支持 JSON 模式时设 0
AI_JSON_MODE=1
AI_TEMPERATURE=0.3
AI_TIMEOUT_MS=120000
